## Hello

This directory contains an example of using the struct template with
licensepack. It iterates over the data at runtime to add color to the output in
more advanced terminals.

To run the code:

```
./build.sh
./hello
./hello -credits
```