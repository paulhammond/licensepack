package {{ .Pkg }}

func init() {
	{{ .Var }} = {{ (eval "licenses" .) | trim | quote }};
}

{{ define "licenses" }}
{{ range .Modules }}
## {{ .Name }}
{{ range .Licenses }}
{{ .Path }}:

{{ .Contents | trim }}

{{ end }}
{{ end }}
{{ end }}
